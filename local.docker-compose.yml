version: "3.9"
services:
  frontend:
    build:
      context: .
      dockerfile: local.Dockerfile
    container_name: doctor-frontend
    ports:
      - "3000:3000"  
    volumes:
      - ./:/app
      - /app/node_modules
    command: npm run dev  
